export const SERIES = {
  'REACT DEEP DIVE': 'react-deep-dive',
} as const;

export const SERIES_NAME = Object.keys(SERIES) as (keyof typeof SERIES)[];


export const SERIES_TITLE = ['react-deep-dive'];
